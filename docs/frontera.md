Persistí la regla en blog-api/.cursor/rules/contract-ready.mdc y CLAUDE.md (alwaysApply), y el agente la cumplió en los handlers nuevos de ese repo.
Dejó de cumplirse al abrir sesión desde blog-ai sin la regla en ese árbol y sin comentarios vecinos que copiar: `latido()` en blog-ai/app/main.py salió sin `# contract-ready` (lo comprobé abriendo el handler; no se puso nada en rojo), mientras que `latido()` en blog-api sí llevó `// contract-ready` porque ahí sigue la regla.
